node_modules/
.env
.env.*
!.env.example
coverage/
logs/
*.log
dist/
.DS_Store
playwright/.auth/

# Backup temporaire de constantes médicales écrit par l'audit de persistance
# (données médicales — ne jamais committer)
tests/.audit-const-backup.json

# Artefacts Playwright
test-results/
playwright-report/

# Artefacts harnais de test (sorties générées à chaque run)
artifacts/*.json
artifacts/*.log

# WhatsApp-Web session et cache (ne jamais committer)
.wwebjs_auth/
.wwebjs_cache/
qrcode-*.png

# Scripts de diagnostic / exploration one-shot (ne jamais committer)
check_*.js
audit_*.js
diagnostic_*.js
seed_*.js
find_*.js
fix_*.js
migration_communaute_*.js
migration_communaute_*.sql
test_*.js
server_diag.log

# Rapports de session temporaires
TABLEAU_*.md
docs/AUDIT_PERSISTANCE_AUTO.md

# Dumps SQL de sauvegarde (potentiellement données patient — ne jamais committer)
backups/
